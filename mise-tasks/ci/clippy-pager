#!/usr/bin/env bash
#MISE description="PR lane: cargo clippy -p xai-grok-pager-bin -D warnings"
#MISE dir="{{ config_root }}"

set -euo pipefail

# shellcheck source=tools
source "$(dirname "$0")/tools"
cargo clippy --locked -p xai-grok-pager-bin -- -D warnings
